include "ActionInput.fbs";
include "PointerInput.fbs";
include "HandInput.fbs";
namespace StardustXR;

union InputDataRaw {
	Pointer,
	Hand,
	Action
}

table InputData {
        uuid:string;
	input:InputDataRaw (required);
	distance:float = 0;
	datamap:[ubyte] (flexbuffer);
}

root_type InputData;
